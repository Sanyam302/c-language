#include<stdio.h>

int main(){
    int length , breadth;
    printf("enter length and breadth");
    scanf("%d %d", &length, &breadth);
    printf("permetre of rectangle %d",2*(length*breadth));
    return 0;
}